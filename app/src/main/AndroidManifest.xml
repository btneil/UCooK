<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ucook">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon2"
        android:supportsRtl="true"
        android:theme="@style/Theme.UCooK">
        <activity android:name=".AfficherPanier"></activity>
        <activity android:name=".afficher_panier" />
        <activity android:name=".AddActivity" />
        <activity android:name=".MenuPrincipal" />
        <activity
            android:name=".AffichageRecette"
            android:label="Details"
            android:parentActivityName=".MenuPrincipal" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>